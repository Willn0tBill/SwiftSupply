document.addEventListener('supabase-ready',loadProducts);document.addEventListener('DOMContentLoaded',()=>{if(window.sb)loadProducts();const search=document.getElementById('search');search?.addEventListener('input',render);document.querySelectorAll('.filter').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');filter=b.dataset.filter;render()}));document.getElementById('stockOnly')?.addEventListener('change',render);});
let products=[];let filter='All';let brandFilter='All';
function money(v){return '$'+Number(v).toFixed(2)}
async function loadProducts(){if(!window.sb)return;const status=document.getElementById('shopStatus');try{const {data,error}=await sb.from('products').select('*').eq('active',true).order('brand').order('name');if(error)throw error;products=data||[];buildBrandFilters();status.textContent=products.length?'Live inventory connected.':'No products are currently listed.';render();}catch(e){console.error(e);status.textContent='The live catalog is not available right now. Please try again later.';}}
function buildBrandFilters(){const row=document.getElementById('brandFilters');if(!row)return;const brands=[...new Set(products.map(p=>p.brand).filter(Boolean))].sort();row.innerHTML=['All',...brands].map(b=>`<button class="filter brand-filter ${b===brandFilter?'active':''}" data-brand="${esc(b)}">${esc(b)}</button>`).join('');row.querySelectorAll('[data-brand]').forEach(b=>b.addEventListener('click',()=>{row.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');brandFilter=b.dataset.brand;render()}));}
function render(){const q=(document.getElementById('search')?.value||'').toLowerCase().trim(),stockOnly=document.getElementById('stockOnly')?.checked;const list=products.filter(p=>(filter==='All'||p.category===filter)&&(brandFilter==='All'||p.brand===brandFilter)&&(!q||[p.name,p.description,p.category,p.brand,p.flavor].join(' ').toLowerCase().includes(q))&&(!stockOnly||p.stock>0));const root=document.getElementById('products'),empty=document.getElementById('empty');root.innerHTML=list.map(p=>`<article class="product-card reveal visible"><a class="product-image" href="product.html?id=${encodeURIComponent(p.id)}">${p.image_url?`<img src="${esc(p.image_url)}" alt="${esc(p.name)}">`:'<span>SwiftSupply</span>'}</a><div class="product-body"><span class="product-category">${esc(p.brand||p.category)}${p.flavor?` · ${esc(p.flavor)}`:''}</span><h3><a href="product.html?id=${encodeURIComponent(p.id)}">${esc(p.name)}</a></h3><p>${esc(p.description||'')}</p><div class="product-bottom"><strong>${money(p.price)}</strong><span class="stock-pill ${p.stock>0?'in':'out'}">${p.stock>0?p.stock+' in stock':'Out of stock'}</span></div>${p.bundle_label?`<small>${esc(p.bundle_label)}</small>`:''}<div class="button-row"><button class="button button-primary add-cart" data-id="${p.id}" ${p.stock<1?'disabled':''}>${p.stock>0?'Add to Cart':'Unavailable'}</button><a class="button button-secondary" href="product.html?id=${encodeURIComponent(p.id)}">Details</a></div></div></article>`).join('');empty.hidden=!!list.length;root.querySelectorAll('.add-cart').forEach(b=>b.onclick=()=>{const p=products.find(x=>x.id===b.dataset.id);if(!p)return;SwiftCart.add(p,1);b.textContent='Added';setTimeout(()=>b.textContent='Add to Cart',900)});}
function esc(s){return String(s??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[m]))}
