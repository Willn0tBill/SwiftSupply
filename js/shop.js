document.addEventListener('supabase-ready',loadProducts);document.addEventListener('DOMContentLoaded',()=>{const params=new URLSearchParams(location.search);brandFilter=params.get('brand')||'All';if(window.sb)loadProducts();const search=document.getElementById('search');search?.addEventListener('input',render);document.querySelectorAll('.filter').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');filter=b.dataset.filter;render()}));document.getElementById('stockOnly')?.addEventListener('change',render);});
let products=[];let filter='All';let brandFilter='All';
function money(v){return '$'+Number(v).toFixed(2)}
async function loadProducts(){if(!window.sb)return;const status=document.getElementById('shopStatus');try{const {data,error}=await sb.from('products').select('*').eq('active',true).order('brand').order('name');if(error)throw error;products=data||[];if(brandFilter!=='All'&&!products.some(p=>(p.brand||'').toLowerCase()===brandFilter.toLowerCase()))brandFilter='All';buildBrandFilters();status.textContent=products.length?'Live inventory connected.':'No products are currently listed.';render();}catch(e){console.error(e);status.textContent='The live catalog is not available right now. Please try again later.';}}
function buildBrandFilters(){const row=document.getElementById('brandFilters');if(!row)return;const brands=[...new Set(products.map(p=>p.brand).filter(Boolean))].sort();row.innerHTML=['All',...brands].map(b=>`<button class="filter brand-filter ${b.toLowerCase()===brandFilter.toLowerCase()?'active':''}" data-brand="${esc(b)}">${esc(b)}</button>`).join('');row.querySelectorAll('[data-brand]').forEach(b=>b.addEventListener('click',()=>{row.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');brandFilter=b.dataset.brand;render()}));}
function render(){const q=(document.getElementById('search')?.value||'').toLowerCase().trim(),stockOnly=document.getElementById('stockOnly')?.checked;const list=products.filter(p=>{const available=Math.max(0,Number(p.stock||0)-SwiftCart.reserved(p.id));return (filter==='All'||p.category===filter)&&(brandFilter==='All'||(p.brand||'').toLowerCase()===brandFilter.toLowerCase())&&(!q||[p.name,p.description,p.category,p.brand,p.flavor].join(' ').toLowerCase().includes(q))&&(!stockOnly||available>0)});root.innerHTML=list.map(p=>{const available=Math.max(0,Number(p.stock||0)-SwiftCart.reserved(p.id));return `<article class="product-card reveal visible"><a class="product-image" href="product.html?id=${encodeURIComponent(p.id)}">${p.image_url?`<img src="${esc(p.image_url)}" alt="${esc(p.name)}">`:'<span>SwiftSupply</span>'}</a><div class="product-body"><span class="product-category">${esc(p.brand||p.category)}${p.flavor?` · ${esc(p.flavor)}`:''}</span><h3><a href="product.html?id=${encodeURIComponent(p.id)}">${esc(p.name)}</a></h3><p>${esc(p.description||'')}</p><div class="product-bottom"><strong>${money(p.price)}</strong><span class="stock-pill ${available>0?'in':'out'}">${available>0?available+' in stock':'In your cart / unavailable'}</span></div>${p.bundle_label?`<small>${esc(p.bundle_label)}</small>`:''}<div class="button-row"><button class="button button-primary add-cart" data-id="${p.id}" ${available<1?'disabled':''}>${available>0?'Add to Cart':'Unavailable'}</button><a class="button button-secondary" href="product.html?id=${encodeURIComponent(p.id)}">Details</a></div></div></article>`}).join('');empty.hidden=!!list.length;root.querySelectorAll('.add-cart').forEach(b=>b.onclick=()=>{const p=products.find(x=>x.id===b.dataset.id);if(!p)return;const result=SwiftCart.add(p,1);if(result.ok){b.textContent='Added';render();setTimeout(()=>{if(document.body.contains(b)){b.textContent='Add to Cart'}},900)}else{b.textContent='Out of Stock';setTimeout(()=>render(),900)}});}
function esc(s){return String(s??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[m]))}
