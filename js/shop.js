document.addEventListener('supabase-ready',loadProducts);document.addEventListener('DOMContentLoaded',()=>{if(window.sb)loadProducts();});
let products=[];let filter='All';
function money(v){return '$'+Number(v).toFixed(2)}
async function loadProducts(){if(!window.sb)return;const status=document.getElementById('shopStatus');try{const {data,error}=await sb.from('products').select('*').eq('active',true).order('category').order('name');if(error)throw error;products=data||[];status.textContent=products.length?'Live inventory connected.':'No products are currently listed.';render();}catch(e){console.error(e);status.textContent='The live catalog is not available yet. Please check back soon.';}}
function render(){const q=(document.getElementById('search')?.value||'').toLowerCase().trim();const list=products.filter(p=>(filter==='All'||p.category===filter)&&(!q||p.name.toLowerCase().includes(q)));const root=document.getElementById('products');const empty=document.getElementById('empty');root.innerHTML=list.map(p=>`<article class="product-card reveal visible"><div class="product-image">${p.image_url?`<img src="${esc(p.image_url)}" alt="${esc(p.name)}">`:'<span>SwiftSupply</span>'}</div><div class="product-body"><span class="product-category">${esc(p.category)}</span><h3>${esc(p.name)}</h3><p>${esc(p.description||'')}</p><div class="product-bottom"><strong>${money(p.price)}</strong><span class="stock-pill ${p.stock>0?'in':'out'}">${p.stock>0?p.stock+' in stock':'Out of stock'}</span></div>${p.bundle_label?`<small>${esc(p.bundle_label)}</small>`:''}<button class="button button-primary product-order" data-id="${p.id}" ${p.stock<1?'disabled':''}>${p.stock>0?'Request Order':'Unavailable'}</button></div></article>`).join('');empty.hidden=!!list.length;root.querySelectorAll('.product-order').forEach(b=>b.onclick=()=>{location.href='bulk-orders.html?product='+encodeURIComponent(products.find(p=>p.id===b.dataset.id)?.name||'')});}
function esc(s){return String(s??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[m]))}
document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.filter').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');filter=b.dataset.filter;render()}));document.getElementById('search')?.addEventListener('input',render);});
